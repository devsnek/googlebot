<script>
  (function() {
    const ws = new WebSocket(document.location.origin.replace('http:', 'ws:').replace('https:', 'wss:'));
    ws.onmessage = (m) => console.log('[WS MESSAGE]', m.data);
    ws.onclose = (e) => console.log('[WS CLOSE]', e.code, e.reason);
  }());
</script>
